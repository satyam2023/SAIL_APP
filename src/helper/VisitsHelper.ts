import { IUpcomindDetail, VisitResponse } from "models/ApiResponses/VisitResponse";
import { UpcomingVisitDetails } from "@shared-constants";

export const generateUpcomingVisitDetail=(upcomingata:IUpcomindDetail)=>{
    //  const Value=Object.entries(upcomingata);

//   for (let val in upcomingata){
//     console.log("Value of upcoming Data::",val);
//   }

console.log("Values::::??????",upcomingata);
}
